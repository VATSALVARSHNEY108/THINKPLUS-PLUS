export default function SequenceToSequence() {
  return (
    <section className="px-12 py-24">
      <header className="max-w-3xl">
        <h1 className="text-3xl font-semibold">Sequence-to-Sequence (Seq2Seq)</h1>
        <p className="mt-4 text-base">
          Seq2Seq maps an input token sequence to an output token sequence using an encoder-decoder architecture. Attention improves this by letting the
          decoder focus on the most relevant encoder states at each generation step.
        </p>
      </header>
      <p>coming soon</p>
    </section>
  );
}

